[server]
headless = true
address = "0.0.0.0"
port = 8501
maxUploadSize = 10

[browser]
gatherUsageStats = false

[client]
# Hide Deploy + developer/settings menu (local and production).
toolbarMode = "minimal"
showErrorDetails = false
